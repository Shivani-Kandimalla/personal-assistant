# Open-Meteo requires NO API key — this file is here for the optional
# v2 stretch goal only (LLM-powered advice wording). Leave blank for v1.

# Optional, only used if you enable the --llm stretch flag later:
# ANTHROPIC_API_KEY=

# Optional config overrides:
# DEFAULT_CITY=Katy, TX
# CALENDAR_PATH=./calendar.json
# WEATHER_API_TIMEOUT_SECONDS=10
